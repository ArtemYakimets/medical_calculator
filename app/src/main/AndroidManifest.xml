<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.medicine_calculator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Medicine_calculator">
        <activity android:name=".info"></activity>
        <activity android:name=".dav" />
        <activity android:name=".kal" />
        <activity android:name=".MainActivity" />
        <activity android:name=".voda" />
        <activity android:name=".stela" />
        <activity android:name=".kurenie" />
        <activity android:name=".rasstvor" />
        <activity android:name=".KA" />
        <activity android:name=".IMT" />
        <activity android:name=".avt">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>